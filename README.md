# react-native-video-android-example

Instructions
Run yarn
Run yarn start
Run android-dev

Maybe mac only, do the following if issues?
Create file local.properties in android, add the following:
sdk.dir = /Users/<YOUR_USERNAME>/Library/Android/sdk
